import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        color: '#9A9FB0',
        textAlign: 'center',
        fontWeight: '600',
    }
});

export default styles;